import { VotingResultPipe } from './voting-result.pipe';

describe('VotingResultPipe', () => {
  it('create an instance', () => {
    const pipe = new VotingResultPipe();
    expect(pipe).toBeTruthy();
  });
});
